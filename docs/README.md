# Assets for this showcase

`architecture.svg` is generated and already referenced from the main README.

## Screenshots to add

- `home.png`
- `download-flow.png`

## Before you commit an image

- Blur or replace real customer names, emails, phone numbers and order IDs
- Remove internal hostnames and staging URLs from the address bar
- Crop out anything showing a client's branding you can't disclose
- Keep width around 1400px — larger is wasted, smaller looks soft on retina
